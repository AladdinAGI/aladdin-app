export interface Dictionary {
  nav: {
    home: string;
    staking: string;
    marketplace: string;
    professional: string;
  };
  button: {
    connect_wallet: string;
  };
  alert: {
    coming_soon: string;
  };
}
